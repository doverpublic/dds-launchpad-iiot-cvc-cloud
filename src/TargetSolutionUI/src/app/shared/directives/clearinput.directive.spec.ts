import { ClearinputDirective } from './clearinput.directive';

describe('ClearinputDirective', () => {
  it('should create an instance', () => {
    const directive = new ClearinputDirective();
    expect(directive).toBeTruthy();
  });
});
